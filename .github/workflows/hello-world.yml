name: Hello World workflow

on: 
  pull_request:
    branches:
      - feature-branch
  push:
    branches: [ "master" ]
jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: print Hello World
        run: echo "Hello World" && sleep 20
      - name: print hello New Era Solution    
        run:  echo "Hello New Era Solutions" && sleep 20
